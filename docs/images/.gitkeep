# Place screenshots referenced by README.md here:
#   function-shortcut.png   - the Controllers > Function Shortcut screen (hero image)
#   before.png              - stock menu rejecting a lone Legion Space binding ("Setup failed")
#   after.png               - a function bound to just Legion Space; row reads "Check quick access shortcuts below"
#   boot-auto-setting.png   - the "Boot automatically into Legion Space" toggle set to Off
